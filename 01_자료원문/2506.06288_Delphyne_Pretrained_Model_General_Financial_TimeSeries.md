# Delphyne: A Pre-trained Model for General and Financial Time Series
- 식별자: arXiv:2506.06288
- URL: https://arxiv.org/abs/2506.06288 (원문 확보: https://arxiv.org/html/2506.06288v1)
- 저자: Xueying Ding (Carnegie Mellon University), Aakriti Mittal (Bloomberg), Achintya Gopal (Bloomberg)
- 수집일: 2026-08-12
- 수집 상태: 완전 (Abstract부터 7 Conclusions까지 본문 전체 원문 확보. References 및 부록 A–J은 생략/개요만)

## Abstract

Time-series data is a vital modality within data science communities. This is particularly valuable in financial applications, where it helps in detecting patterns, understanding market behavior, and making informed decisions based on historical data. Recent advances in language modeling have led to the rise of time-series pre-trained models that are trained on vast collections of datasets and applied to diverse tasks across financial domains. However, across financial applications, existing time-series pre-trained models have not shown boosts in performance over simple finance benchmarks in both zero-shot and fine-tuning settings. This phenomenon occurs because of a i) lack of financial data within the pre-training stage, and ii) the negative transfer effect due to inherently different time-series patterns across domains. Furthermore, time-series data is continuous, noisy, and can be collected at varying frequencies and with varying lags across different variables, making this data more challenging to model than languages. To address the above problems, we introduce a Pre-trained MoDEL for FINance TimE-series (Delphyne). Delphyne achieves competitive performance to existing foundation and full-shot models with few fine-tuning steps on publicly available datasets, and also shows superior performances on various financial tasks.

## 1 Introduction

Time series is one of the most ubiquitous modalities in finance. Time-series analysis is critical to various tasks, such as asset pricing, volatility modeling, risk management, economic indicator analysis, etc. Traditional statistical methods for financial time series include Autoregressive Integrated Moving Average (ARIMA), Generalized Autoregressive Conditional Heteroskedasticity (GARCH), and Vector Autoregressive (VAR) models. In recent years, deep learning-based methods are being applied to these financial tasks.

Following the surge of large language models (LLMs), research has explored the possibility of large deep-learning models for time-series analysis. One approach focuses on prompting and adapting existing pre-trained language models for time-series tasks, while another line of research trains foundation models directly on time-series data. Both approaches aim to train a single model with large-scale cross-domain data that can be applied to various tasks, as opposed to full-shot methods that require training a distinct model for each dataset.

However, previous research indicates that directly prompting LLMs for financial tasks brings only modest benefits; moreover, most LLM-based research focuses only on forecasting directional trends of stock returns, offering fewer practical applications than traditional methods. Preliminary experiments also reveal that pre-trained models do not demonstrate significant improvement over GARCH in risk analysis (Sec. 5).

One reason for this limitation is that the existing public datasets for pre-training lack sufficient financial data to learn patterns and distributional properties that are specific to time-series data in finance. Jointly training on financial data alongside public datasets presents an additional challenge: the substantial differences between domains can lead to *negative transfer*. Negative transfer has been extensively discussed within the literature, defined as a case when "transferring knowledge from the training data has an negative impact on the target tasks" (Wang et al., 2019). Negative transfer is identified as a key obstacle towards pre-training graph foundation model, yet is not explored within the context of time-series data. In Sec. H (appendix), the paper shows that the negative transfer effect is a real challenge when pre-training time-series models. Cross-domain transfer learning is particularly difficult, as time-series data tends to be noisier and more continuous compared to languages and images. To alleviate the negative transfer effect, the paper argues that fine-tuning is the only remedy. The strength of pre-trained time-series models lies in their capacity to rapidly "unlearn" the biases of the pre-training stage and "adapt" to the specific distribution of new tasks, given limited training data and time.

Building on this observation and analysis of negative transfer, the authors introduce their pre-trained time-series model, which incorporates several architectural modifications designed to better handle time-series data and tasks. Time-series data often involve an arbitrary number of variates, each collected at different frequencies (e.g., where quarterly profit growth is forecasted from daily or weekly sales data). Additionally, finance tasks often involve nowcasting, where some variates contain contemporaneous data. The model is an encoder-based transformer that employs the any-variate attention mechanism, introduced by Woo et al. (2024). In addition, they implement a missing data mask alongside the forecast mask, which enhances the model's ability to adapt to cross-frequency training while minimizing negative interference.

Probabilistic forecasting is an essential feature required for uncertainty quantification and anomaly detection within time series. Existing literature either assumes a fixed output distribution (Salinas et al., 2020a) or utilizes a mixture of several distributions (Woo et al., 2024). However, when the underlying data exhibits contrasting distributions and supports, a single distribution may be inadequate. The model simplifies the output by using a mixture of Student-T distributions, which are widely used in finance for heavy-tailed tasks. In preliminary studies, this approach shows comparable performance to Woo et al. (2024) on downstream tasks.

The paper trains its Pre-Trained MoDEL for FINance TimE-series (Delphyne), the first time-series model capable of both general and finance-specific tasks. Delphyne is trained on both LOTSA (the largest public time-series dataset) and financial data (Sec.4). Experiments for both general and finance time-series tasks demonstrate that Delphyne consistently outperforms or matches state-of-the-art baselines. Key contributions:

- Demonstrate the presence of negative transfer in pre-trained time-series models, highlighting how this effect sets them apart from LLMs, emphasizing that the value of pre-trained time-series models lies in their fine-tuning capability, which mitigates the negative transfer effect and enhances downstream performance with minimal iterations.
- Introduce several architectural modifications in the model and demonstrate their significance through comprehensive ablation studies.
- Delphyne is the first time-series pre-trained model to excel in both general time-series tasks and a variety of downstream finance tasks.

## 2 Negative Transfer Effect

Negative transfer has been widely studied in foundation models across different modalities. This problem is typically seen as the model's reduced performance on downstream tasks due to mismatches between the source training data and the target distribution. However, this phenomenon has not been thoroughly explored in the context of time series. In pre-trained time-series models, negative transfer can occur when cross-domain data is added during pre-training. The appearance of data from too different distributions can lead to less effective zero-shot forecast results, even when feeding the downstream tasks within similar domains. Three examples are presented to highlight the presence of negative transfer, particularly focusing on the difficulties of cross-domain transfer from finance data to other areas.

**Pre-training with GARCH and Wavelet Data.** To simulate datasets encountered in real-world scenarios, two types of synthetic data are generated: Wavelet functions and GARCH-style data. Wavelet functions are composed of a combination of sine and cosine waves, while in GARCH models the current time-steps are based on past squared residuals and past variances, frequently used to capture volatility clustering seen in financial time-series data.

Three distinct models were trained: the first on GARCH-style data (G-Model), the second on Wavelet data (W-Model), and the third on a combination of both, using half Wavelet and half GARCH data (M-Model). Each model utilizes a standard autoregressive transformer decoder without any additional embeddings or patching. Table 1 presents the negative log-likelihood (NLL) for each model's zero-shot forecasts with context lengths of 32 and 128. As expected, the mixed-data model (M-Model), trained on both GARCH and Wavelet data, performs significantly worse in terms of zero-shot NLL compared to the models trained on a single data source. This underperformance is likely due to the differing frequencies and structures of GARCH and Wavelet data.

**Table 1 — Zero-shot NLL(↓) for models trained on different data types:**

| Model | Wavelet Pred. | GARCH Pred. |
|---|---|---|
| G-Model-128 | - | 0.0865 |
| W-Model-128 | -0.1020 | - |
| M-Model-128 | -0.0733 | 0.1137 |
| G-Model-32 | - | 0.0882 |
| W-Model-32 | -0.1330 | - |
| M-Model-32 | -0.0732 | 0.1176 |

**Bayesian MCMC.** While the previous example showcases that a model trained on both GARCH and Wavelet data underperforms compared to models trained on a single type, one may argue that it is a model training problem. However, further evidence shows that negative transfer occurs because of the contrasting properties of the data, regardless of the model used.

The time-series pre-trained models are analyzed in a Bayesian framework, in analogy to the Bayesian inference mechanism in Müller et al. (2022) and Hollmann et al. (2023). The pre-training stage is essential to fit a good prior to the training tasks. Given N tasks (datasets) $\{\mathcal{D}_i\}_{i=1}^N$, the learned parameters $\hat{\mathbf{w}}$ implicitly present the maximum likely distribution over all observed tasks by minimizing the pre-training objective. As shown by Müller et al. (2022), optimizing the following training loss approximates the true Bayesian posterior forecast distribution for time series:

$$\mathbb{E}_{x_{1:t+1} \sim p(D)} \left[ -\log q_{\theta}(x_{t+1}|x_{1:t}) \right] \quad (1)$$

In light of this framework, the results of zero-shot learning can be simulated by setting the prior to some data generating process (instead of learning from data). To illustrate this approach, a model was first constructed to simulate the data-generating processes of Wavelet functions, and then corresponding observed data were generated. Markov Chain Monte Carlo (MCMC) is subsequently applied to obtain posterior samples for the model (the distribution of model parameters after conditioning on the observed data). The Wavelet model is fully aware of the underlying data-generating process, and it should be capable of modeling the distribution accurately, apart from the additive Gaussian noise. Then, a mixture of Wavelets and GARCH is fitted to the same data (Appendix H.2).

NLL and Mean Squared Error (MSE) were computed for the fitted Wavelet-MCMC model and the Mixture-MCMC model. Despite having full knowledge of the underlying data generation processes for both the Wavelet and GARCH functions, the mixture model struggles to accurately approximate the posterior distribution in zero-shot forecasts, resulting in higher NLL and MSE compared to models trained on a single function type. This illustrates the phenomenon of negative transfer, where incorporating too many variations of data can negatively impact the modeling of the posterior distribution.

**Table 2 — Forecasted NLL(↓) and MSE(↓) for Wavelet-MCMC and Mixture-MCMC model:**

| Model | MSE | NLL |
|---|---|---|
| Wavelet-MCMC | **0.1058** | **0.2692** |
| Mixture-MCMC | 0.1127 | 0.2937 |

**Delphyne Training.** Similar effects are observed while training the Delphyne model. During the pretraining phase, different checkpoints are evaluated to assess the zero-shot forecast performance on the ETTh2 dataset for two versions of the model: Delphyne-A (trained with the LOTSA and financial data) and Delphyne-L (trained without financial data).

The average Mean Absolute Error (MAE) across forecast lengths of 96 and 196 (196 as printed in source; likely 192) is recorded. Additionally, the fine-tuning performance of both models is evaluated using MAE as the loss objective. Initially, Delphyne-A incurs a higher MAE than Delphyne-L, but after fine-tuning, both models achieve comparable MAE. For in-distribution forecasting on the Monash dataset, Delphyne-A also underperforms Delphyne-L with larger aggregated MAEs. However, both methods perform comparably after fine-tuning (Sec. 5.2).

**A Framework to Relate Pre-training to Downstream Performance.** The three examples above demonstrate that negative transfer can indeed occur when pre-training time-series data from various domains. The continuous nature of time-series data, combined with its inherent noise and significant variability across domains, makes pre-training a model for time-series particularly challenging compared to other modalities like language and images.

Given the negative transfer phenomenon, why build a pre-trained time-series model at all? The strength of a pre-trained time-series model lies in its ability to quickly adapt to downstream tasks with fine-tuning on only a few training samples, which offers great time and data efficiency in comparison to full shot models. The goal is to develop a pre-trained time-series model that can swiftly adjust to new task distributions by efficiently "unlearning" pre-training biases and "adapting" to the specific characteristics of downstream tasks, even with limited training data or minimal gradient updates.

## 3 Model Overview

**Problem Formulation.** For any-variate time-series, assuming that each dataset $\mathcal{D} = \{\mathbf{Y}^{(i)}\}_{i=1}^M$ has M data points, while each $\mathbf{Y}^{(i)} \in \mathbb{R}^{l(i) \times T_{\mathbf{Y}(i)}}$ contains $l(i)(l(i) \geq 1)$ variates and $T_{\mathbf{Y}(i)}$ time-steps. The pre-training is formulated as a forecasting task: for each variate j, the future $h_j \geq 0$ time-steps are forecasted by modeling their forecast distribution: $P(\mathbf{Y}_{T_{\mathbf{Y}}-\mathbf{h}:T_{\mathbf{Y}}}|\phi)$, where $\phi$ is the output distribution of the time-series forecasting model and $\mathbf{h}$ is a vector comprised of $h_j$ time-steps. The overall training objective is:

$$\min_{\mathbf{w}} \mathbb{E}_{P(\mathbf{Y}) \sim \mathcal{D}} \left[ -\log P(\mathbf{Y}_{T_{\mathbf{Y}} - \mathbf{h}: T_{\mathbf{Y}}} | \phi) \right] \quad \text{s.t. } \phi \leftarrow f_{\mathbf{w}}(\mathbf{Y}_{T_{\mathbf{Y}} - p: T_{\mathbf{Y}} - \mathbf{h}}) \quad (2)$$

where $\mathbf{h}$ is defined as the forecast length and p as the look-back window length. The number of time-steps conditioned on for each time series is different: $h_j-p$. The problem is formulated this way so that the number of time-steps to forecast is different per time series in order to allow for nowcasting where the information available for each time series can be different.

Time-series data, particularly in finance, exhibit unique characteristics that present distinct challenges:

1. **Multivariate Nature**: The variable Y typically has a dimensionality k > 1, indicating the presence of multiple interrelated time series. For example, US stocks are often quite correlated with the S&P 500 index.
2. **Nowcasting Data**: The data of some variables is often available with considerable time lag, so it is important to estimate the current value of the time-series based on its own recent history and current values of other variables. This is the nowcasting task in economics and financial applications.
3. **Multifrequency and Missing Data**: Each Y variable can be collected at varying granularities—such as individual tick data versus aggregated bar data—or may contain numerous missing entries due to irregular sampling intervals.
4. **Extended Context Length**: Unlike natural languages, where input sequences are generally in the range of hundreds of tokens, financial time-series data often span thousands of timesteps, requiring models to handle significantly longer temporal dependencies.

### 3.1 Overall Architecture

Following recent approaches (Woo et al., 2024; Goswami et al., 2024), a transformer encoder structure is adopted as the backbone of Delphyne.

**Padding.** To handle the multivariate nature of time-series data and accommodate the flexibility of varying the number of variates and covariates, all variates are flattened and concatenated along a single dimension. Each variate is assigned a unique identifier, and instance normalization is applied independently. For shorter time-series data, the time series is padded on the right (after forecast mask); prior literature pads to the left, but since the model needs to handle time series where each can have a variable length, right-padding is required.

**Missing and Forecast Masking Before Patching.** Recent work shows that patching time series allows the model to attend to significantly longer contexts. Therefore, Delphyne breaks the flattened time series into disjoint patches, fixing the patch size to 32. Contrary to prior work, the model creates a [FORECAST MASK] to identify the target timesteps for forecast as well as a [MISSING MASK] that indicates where data is unobserved due to the sampling procedure. For many finance tasks, data is collected at multiple frequencies or sampled irregularly, often leading to missing time steps (e.g., daily stock prices not being recorded on holidays). Simply ignoring these gaps can misalign multifrequency data, while backfilling or zero-filling distorts the original data distribution and can produce unsatisfactory forecasts. Missing data is treated similarly to forecast masks during training; however, missing values are excluded from the forecast process. Both masks are applied alongside the time-series data and a trainable linear projection is learned to generate time-series embeddings that incorporate both data and missingness information. These patch embeddings serve as input to the transformer model.

**Attention Block.** The transformer encoder utilizes pre-normalization (Xiong et al., 2020), rotary positional embedding (Su et al., 2024), any-variate attention (Sec. 3.3), Silu activation function, and gated linear unit (GLU) to replace FFN.

### 3.2 Context Length and Masking Ratio

The Delphyne architecture requires careful tuning of key training parameters, like the masking ratio during pre-training. While vision transformers use around 60% (He et al., 2022), language models use 15%, and prior pre-trained time-series models range from 30% to 50%. This ratio is crucial as it affects input context length, impacting generalization and fine-tuning. Ablation studies explore the impact of masking ratios and context lengths on training efficiency.

**Ablation 1.** The first experiment explores how varying pre-training context lengths affects both pre-training and fine-tuning performance. Small- and medium-size autoregressive transformer decoders are trained on synthetic Wavelet data with context lengths of {16, 32, 64, 128}. After convergence, zero-shot NLL forecast is assessed with a fixed context length of 32. Fine-tuning is performed with varying amounts of data, consistently using a context length of 32. Table 3 reports the NLL for zero-shot and fine-tuning. While pre-training with a context length of 32 yields the best zero-shot performance, a longer context length of 64, 128 improves fine-tuning, especially with fewer fine-tuning (10-100) samples. This suggests that for effective fine-tuning, pre-trained time-series models benefit from longer context lengths during pre-training.

**Table 3 — NLL(↓) for zero-shot and fine-tuning with varying sample sizes:**

| Model | Zero-Shot | 1 Sample | 10 Samples | 100 Samples | 1000 Samples |
|---|---|---|---|---|---|
| Small-16 | -0.0449 | -0.1189 | -0.1523 | -0.1824 | -0.1870 |
| Small-32 | -0.0978 | -0.0842 | -0.1681 | -0.1873 | -0.1898 |
| Small-64 | -0.0808 | -0.1418 | -0.1684 | -0.1861 | -0.1853 |
| Small-128 | -0.0842 | -0.1287 | -0.1825 | -0.1907 | -0.1918 |
| Medium-16 | -0.0483 | -0.0893 | -0.1542 | -0.1767 | -0.1897 |
| Medium-32 | -0.1330 | -0.1486 | -0.1673 | -0.1792 | -0.1847 |
| Medium-64 | -0.0793 | -0.1146 | -0.1612 | -0.1873 | -0.1875 |
| Medium-128 | -0.1020 | -0.1113 | -0.1711 | -0.1843 | -0.1899 |

**Ablation 2.** The maximum masking ratio is varied in {0.25, 0.5, 0.99} to observe the effects on the resulting model, using a transformer encoder on Wavelet data. Fine-tuning performance is evaluated by NLL for various forecast lengths (Table 4). When masking less aggressively, the model consistently performs well across various forecast lengths, consistent with the earlier finding that less aggressive masking implicitly exposes the model to longer context lengths during pre-training, leading to improved fine-tuning performance.

During model pre-training, a masking ratio is applied to each variate, sampled from a beta-binomial distribution with α=5 and β=10; the average masking ratio is around 30%. Each variate is masked independently, enabling the model to better adapt to nowcasting scenarios where variates and covariates may have different lookback window lengths.

**Table 4 — NLL(↓) for varying pretraining masking ratios (selected rows):**

| Pred. Len. | Sample Size | Ratio 0.25 | Ratio 0.5 | Ratio 0.99 |
|---|---|---|---|---|
| 32 | 1 | -0.441 | -0.187 | 0.416 |
| 32 | 1000 | -0.666 | -0.662 | -0.676 |
| 64 | 1 | -0.263 | -0.077 | 0.122 |
| 64 | 1000 | -0.318 | -0.314 | -0.318 |
| 96 | 1 | 0.015 | 0.081 | 0.132 |
| 96 | 1000 | -0.020 | -0.023 | -0.023 |

### 3.3 Training on Multivariate Data

Multivariate time-series modeling poses unique challenges, particularly in capturing correlations between variates. Nie et al. (2023) analyze channel-mixing versus channel-independence approaches, finding that the latter improves adaptability and generalization. Many pre-trained models for multivariate time series adopt channel-independence, while others focus solely on univariate time series. Recently, Woo et al. (2024) introduce any-variate attention (Appendix A), which flattens multivariates into univariates and captures inter-variate correlations via a specialized bias term. Experiments on synthetic Wavelet data compare these design choices.

**Ablation 3.** Two scenarios are modeled: one where the Wavelet data across rows are correlated, and another where they are uncorrelated. In the correlated scenario, the time-series data are generated using the same Wavelet function, differing only by additive Gaussian noise. In the uncorrelated scenario, the data are generated using different Wavelet functions.

Table 5 shows the results. Multivariate models significantly outperform univariate approaches for correlated rows of Wavelet data, underscoring the importance of capturing inter-variable dependencies. Channel-mixing shows superior performance in zero-shot settings and with smaller training samples, but only when training and forecasting with correlated Wavelet data. However, when data rows are uncorrelated, channel-mixing performs worse than the other two models. Given that Delphyne is trained on both strongly and weakly correlated financial data such as the returns of stocks across various sectors, it employs an any-variate attention mechanism. This mechanism enables the integration of cross-channel information without forcibly mixing variate data. Unlike Woo et al. (2024), the authors do not intentionally create multivariate data rows across different datasets.

**Table 5 — NLL(↓) for zero-shot and fine-tuning with varying sample sizes, for different multivariate modeling methods:**

| | Model | Zero-Shot | 1 Sample | 10 Samples | 100 Samples | 1000 Samples |
|---|---|---|---|---|---|---|
| Corr. | Univariate | -0.0978 | -0.0842 | -0.1681 | -0.1873 | -0.1898 |
| Corr. | Channel Mixing | -0.1531 | -0.1650 | -0.1797 | -0.1873 | -0.1913 |
| Corr. | Any-variate Attention | -0.1513 | -0.1507 | -0.1794 | -0.1892 | -0.1895 |
| Uncorr. | Univariate | -0.0978 | -0.0842 | -0.1681 | -0.1873 | -0.1898 |
| Uncorr. | Channel Mixing | -0.0928 | -0.1323 | -0.1722 | -0.1892 | -0.1874 |
| Uncorr. | Any-variate Attention | -0.0922 | -0.1386 | -0.1879 | -0.1879 | -0.1886 |

### 3.4 Output Distribution

For probability forecasting, previous studies usually assume a fixed output distribution (Salinas et al., 2020a). However, when the data presents varying distributions and supports, relying on a single distribution may be insufficient. Woo et al. (2024) propose a mixture of a Student's T-distribution, Log-normal, Gaussian, and Negative Binomial, which introduces asymmetry into the forecasted distribution and demonstrates improved results. An additional experiment studies the effect of various distributions.

**Ablation 4.** With a fixed training regime and transformer backbone, a stock return's NLL (Sec. 5.1) is measured across three output distributions: a single distribution, a Student's T mixture, and a mixture of different distributions as in Woo et al. (2024), called MOIRAI's Mixture. Figure 4 shows that the Student's T mixture performs similarly to MOIRAI's Mixture on measuring stock returns. On the other hand, a single distribution underperforms both. For simplicity (Occam's Razor), the Student's T mixture is chosen as the output distribution.

## 4 Training Details

### 4.1 Training Data

Both publicly available data and financial data are included in Delphyne's training, allowing Delphyne to generalize well to daily time-series forecast tasks as well as financial time-series tasks.

**LOTSA Data** (Woo et al., 2024) is the largest publicly available dataset for pre-training large time-series models, containing 231 billion observations across domains like energy, transport, climate, CloudOps, web, sales, nature, economics, finance, and healthcare. It aggregates most open source time-series datasets.

**Financial Data.** The financial dataset contains data for companies, stocks, ETFs, currencies (exchange rates), and commodities. To handle multiple frequencies, daily datasets and monthly datasets are created. To ensure that there is no lookahead bias in downstream tasks, the model is pre-trained with data only until the end of 2019 (note: text has a possible inconsistency mentioning "end of 2011" then "end of 2019" — original text reads confusingly across a figure caption; end of 2019 appears to be the intended cutoff).

**Sampling.** The LOTSA dataset is significantly imbalanced, necessitating subsampling to ensure more balanced representation during training. The few datasets that dominate in size are identified and their likelihood of being sampled is reduced to avoid overrepresentation. For any dataset, the total number of observations (across samples, variates, and timesteps) within the dataset is computed, $|\mathcal{D}_k| = \sum_{i=1}^M \sum_{j=1}^{l(k)} T_{i,j}$. The scores are then normalized to sum to one. The weighting is capped to be at least 0.001, normalized again to obtain the final probability of sampling a dataset. Overall, the training data consists of 85% from LOTSA and 15% from financial data (Table 6). The number of variates (≤128) is sampled using a beta-binomial distribution (α=2, β=5).

**Table 6 — Sampling dataset probability (%) across LOTSA domains and finance data:**

LOTSA (total 85%): Energy 17.8, Transport 25.5, CloudOps 8.6, Climate 11.9, Econ/fin 5.7, Web 6.1, Sales 5.4, Nature 3.9, Healthcare 0.2.
Finance Data (total 15%): ETFs 2.8, Tickers 2.0, Commodities 0.3, Currency 0.9, Stock 2.8, Company 1.8, Intraday Bars 2.0 (approximate breakdown per source table, some cell alignment ambiguous in original HTML table extraction).

Unlike other models that involve complex preprocessing like packing, concatenation, or augmentation, this approach is streamlined: only subsampling is applied and time is truncated to 512×32 timesteps. For ease of normalization, entries with fewer than five observations or uniform values after forecast and missing masking are excluded. This keeps the training data robust and optimizes model performance without excessive preprocessing.

### 4.2 Model Parameters

Delphyne is trained with 12 layers and 768-dimensional attention shared across 12 heads, with a maximum width of 3072. Dropout is set to 0.2, and the model is trained on negative log-likelihood (NLL) loss. It is pretrained for 1 million gradient updates with a fixed patch size of 32 and a sequence length of 512×32 steps. Using a batch size of 256, optimization uses AdamW (learning rate = 1e-4, weight decay = 0.1, β1=0.9, β2=0.98) with a learning rate scheduler of 10,000 steps of linear warmup and cosine annealing to 1e-5. Training was conducted on 8 H100 GPUs over 4 days with mixed-precision (bf16).

## 5 Experiments

Three models are trained for downstream evaluations: Delphyne trained on the LOTSA dataset (Delphyne-L), Delphyne trained on finance data only (Delphyne-F), and Delphyne trained with all finance and LOTSA data (Delphyne-A). Zero-shot (ZS) forecasts and fine-tuning (FT) performances are compared on standard short-term and long-term time-series forecasting tasks, probabilistic quantification, anomaly detection tasks, as well as finance forecast tasks for stock risk analysis, stock volatility modeling, intraday traded volume modeling, and company revenue nowcasting.

### 5.1 Financial Tasks

**Stocks.** While a commonly evaluated task for finance is forecasting the returns, modeling the distribution of stock returns is equally important for stress-testing scenarios and conducting risk analysis. Daily stock returns of 14 major stocks from the SPX Index from 2021-01-04 to 2023-12-29 are used for testing. For fine-tuning, data from 1996-07-01 to 2019-12-31 is used for training, 2020-01-02 to 2020-12-31 for validation, with a context length of 252.

Since most methods, except MOIRAI, forecast point estimates, two experiments are conducted: forecasting next-day stock returns' variance (volatility) to compare MSE, and evaluating NLL for the forecasted returns distribution. Benchmarks include GARCH with Student's T (a standard financial baseline). For PatchTST, the output is adapted to a mixture of four Student's T distributions.

Tables 7 and 8 show the overall results (exact per-model row labels not fully resolved in the extracted HTML table structure). The NLL metrics indicate that while utilizing only financial data in pre-training brings the best zero-shot performance (Delphyne-F), Delphyne-A achieves the best results after fine-tuning. The MSE for variance forecasts shows that Delphyne models outperform others and perform better in zero-shot than after fine-tuning — hypothesized to be due to the dataset's high noise leading to overfitting during fine-tuning. Additional evaluation of coverage statistics (calibration performance) confirms that Delphyne models achieve the best performance (Appendix D.1).

**Bars.** Intraday bars data (log of volume traded in five-minute intervals) is used to test long-sequence modeling performance. For 4 different ETFs, the past 15 days of data (context length 15×78) is used to forecast log-volume in the next day's trading hours (forecast length 78).

**Table 9 — MSE for bars (log-volume):**

| Model | MSE ZS | MSE FT |
|---|---|---|
| Delphyne-A | 0.728 | 0.551 |
| Delphyne-F | 0.965 | 0.530 |
| Delphyne-L | 0.930 | 0.557 |
| MOIRAI | 0.767 | 0.620 |
| MOMENT | 0.775 | 0.838 |
| TTM | 0.714 | 0.601 |
| PatchTST | - | 0.534 |
| Avg past values | 0.602 | - |

Results are for 2021-01-04 to 2021-01-11. For fine-tuning, data from 2008-01-24 to 2019-12-30 is used for training, and 2019-12-31 to 2020-12-31 for validation. All Delphyne models significantly improve metrics after fine-tuning, effectively capturing the seasonal component in bar log-volume data. Delphyne-A-ZS outperforms Delphyne-F-ZS and Delphyne-L-ZS due to the data's seasonal nature, similar to electricity and weather datasets. However, with fine-tuning, Delphyne-F achieves the best performance across all methods.

**Nowcasting Company Revenue.** Consumer transaction data is used to test nowcasting performance, forecasting year-over-year (YoY) sales growth for 211 U.S. companies based on 1) previous quarter's YoY sales growth, 2) previous YoY growth in transactions, and 3) current quarter's YoY growth in transactions. Due to the quarterly nature, the context length is short (4-8). Rolling forecasts are made for Q3 2022 to Q1 2023. For fine-tuning, data from 2018 Q1 to 2021 Q1 is used for training, and 2021 Q2 to 2022 Q2 for validation. Comparison is against a statistical baseline built by the providers of the consumer transaction data and MOIRAI.

**Table 10 — Nowcasting results (MAE) for company sales growth:**

| Model | MAE ZS | MAE FT |
|---|---|---|
| Delphyne-A | 0.099 | 0.071 |
| Delphyne-F | 0.128 | 0.079 |
| Delphyne-L | 0.101 | 0.073 |
| MOIRAI | 0.091 | 0.093 |
| Baseline | 0.100 | - |

Delphyne-A with fine-tuning outperforms all methods, and Delphyne-L ranks second despite not seeing the data during pre-training — attributed to Delphyne-L's independent masking of variates during pre-training, which enhances handling of contemporaneous data for nowcasting.

### 5.2 Short-term Forecasting on Monash Dataset

Evaluation is conducted using the Monash dataset, spanning multiple domains like demand forecasting, traffic, and weather, with various data granularities. The train-test split outlined in Woo et al. (2024) is followed, evaluating performance only on the hold-out test set to ensure a fair in-distribution comparison for Delphyne-L and Delphyne-A. For zero-shot, context length is fixed at 500. For fine-tuning, context length is searched in {100,300,500,1000,2000} and learning rate in {5e-5,1e-5}.

Delphyne is compared with statistical baselines, supervised ML baselines, foundation models, and LLM-based models. In zero-shot performance, Delphyne-F, trained solely on finance data, slightly outperforms the Naive model and other benchmarks. Since Monash is out-of-distribution for Delphyne-F, a decline in performance is expected. In contrast, Delphyne-L achieves a lower MAE than Delphyne-A, likely because Delphyne-A's training data contains finance data and may introduce negative transfer effects. After fine-tuning, both Delphyne-A and Delphyne-L show similar performance, rivaling the best model, MOIRAI.

### 5.3 Out-of-distribution Long-term Forecasting

For long-term forecasting, Delphyne and benchmarks are evaluated on popular datasets with forecast lengths of 96, 192, 336, and 720, using the standard ETT datasets, weather, and electricity (ECL) dataset.

**Table 11 — Average MAE and MSE across prediction lengths {96,192,336,720} (selected rows):**

| Dataset | Metric | TTM | MOIRAI | TimesFM | Delphyne-A-ZS | MOMENT | GPT4TS | Delphyne-A-FT | PatchTST | Dlinear | TimesNet | FEDFormer | Stationary |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| ETTh1 | MSE | 0.402 | 0.434 | 0.476 | 0.449 | 0.418 | 0.428 | 0.440 | 0.413 | 0.423 | 0.457 | 0.440 | 0.570 |
| ETTh2 | MSE | 0.327 | 0.346 | 0.404 | 0.375 | 0.352 | 0.355 | 0.352 | 0.330 | 0.431 | 0.414 | 0.437 | 0.526 |
| ETTm1 | MSE | 0.338 | 0.382 | 0.420 | 0.501 | 0.354 | 0.352 | 0.364 | 0.351 | 0.357 | 0.400 | 0.448 | 0.481 |
| ETTm2 | MSE | 0.264 | 0.272 | 0.350 | 0.323 | 0.256 | 0.266 | 0.250 | 0.255 | 0.267 | 0.291 | 0.305 | 0.306 |
| ECL | MSE | 0.160 | 0.188 | 0.156 | 0.202 | 0.165 | 0.167 | 0.170 | 0.162 | 0.166 | 0.193 | 0.214 | 0.193 |
| Weather | MSE | 0.233 | 0.235 | 0.232 | 0.369 | 0.230 | 0.237 | 0.221 | 0.226 | 0.249 | 0.259 | 0.309 | 0.288 |

Delphyne-A underperforms compared to TTM, MOIRAI, and other foundation models in zero-shot settings — attributed to its use of fixed patch sizes rather than dynamic adaptation and Delphyne's training on the negative log-likelihood of a Student's-T mixture distribution, optimized for financial tasks. However, Delphyne-A demonstrates strong performance after fine-tuning, a crucial step for improving out-of-distribution forecasting. Delphyne-A significantly outperforms Delphyne-L on ETTm1 and ETTh1 with a 720-forecast length, with no observed negative transfer — possibly due to shared seasonality trends between ETT and financial (bars) data. Both models achieve similar performance after fine-tuning.

### 5.4 Probability Quantification

Probability forecasting is assessed on six datasets spanning the energy, transport, climate, and sales domains, using a rolling evaluation setup where the stride matches the forecast length. Continuous Ranked Probability Score (CRPS) and Mean Scaled Interval Score (MSIS) metrics are reported (Table 12). Delphyne-A's zero-shot performance is slightly below that of MOIRAI, particularly in terms of CRPS metrics. However, after fine-tuning, Delphyne-A shows significant improvement, achieving the best results across various datasets. Outperformance in MSIS is suspected to be due to Delphyne-A modeling having a heavier-tailed distribution using a mixture of Student's T distributions, but there is still room for selecting a more expressive distribution to improve CRPS, as distributions can be specific to each dataset.

**Table 12 (selected rows, CRPS/MSIS):**

| Dataset | Metric | Delphyne-A-ZS | MOIRAI | Delphyne-A-FT |
|---|---|---|---|---|
| Electricity | CRPS | 0.159 | 0.055 | 0.140±0.005 |
| Electricity | MSIS | 29.293 | 6.172 | 21.820±1.383 |
| Solar | CRPS | 0.905 | 0.419 | 1.306±0.103 |
| Walmart | CRPS | 0.093 | 0.093 | 0.083±0.001 |
| Weather | CRPS | 0.064 | 0.041 | 0.042±0.005 |
| Istanbul Traffic | CRPS | 0.149 | 0.116 | 0.212±0.015 |
| Turkey Power | CRPS | 0.046 | 0.040 | 0.035±0.001 |

(전체 표에는 PatchTST/TiDE/TFT/DeepAR/AutoARIMA/Seasonal Naive 등 추가 baseline 열이 포함되어 있으나, 표 구조 추출 한계로 CRPS 핵심 열만 옮김. 정확한 수치는 원문 표 12 참조 필요.)

### 5.5 Anomaly Detection

Adjusted F1 score is measured for the anomaly detection task on 44 time-series datasets from the UCR anomaly detection archive, compared to popular full-shot models and the foundation model MOMENT with an anomaly detection head. Delphyne-A's versatility allows it to adapt well to anomaly detection tasks after finetuning, achieving second place overall in anomaly detection tasks.

## 6 Related Work

**Transformer-based Time-series Modeling.** Transformers have become a powerful backbone for time-series analysis. Many studies enhance attention mechanisms to handle longer context lengths in time-series data. Nie et al. (2023) show that segmenting time-series data into patches as tokens improves performance and captures representations effectively for forecasting. Liu et al. (2024d) treat independent time series as tokens to capture inter-correlations, enhancing multivariate forecasting.

**Time-series Foundation Models.** Building on the rise of LLMs, recent research has explored the potential of large deep learning models for time-series analysis. One approach adapts existing pre-trained language models for time-series tasks through prompting, while another focuses on training foundation models specifically on time-series data. Both strategies seek to develop a single model trained on large-scale, cross-domain data, capable of addressing multiple forecasting tasks, in contrast to full-shot methods that require training a separate model for each dataset. This work is closely related to foundation models, with several architectural modifications for financial domains.

**Transfer Learning and Distribution Shift in Time-series domains.** The negative transfer effect is widely studied in the transfer learning literature, but remains less explored in the context of pretraining models for time-series modeling. This effect can be viewed through the lens of distribution shifts within training and testing time-series datasets. Several studies have proposed methods to address distribution shifts in time-series tasks. This work differs from the distribution shift literature; the aim is to develop a pre-trained time-series model capable of rapidly adapting to downstream tasks through few-shot fine-tuning.

## 7 Conclusions

The presence of negative transfer effect is illustrated in pre-trained time-series models especially when pre-trained with time-series data from various domains, contrasting them with LLMs for language tasks. Experiments emphasize the role of fine-tuning to counter this effect, which helps pre-trained time-series models adapt to diverse downstream tasks with few training samples and minimal iterations. Various architectural modifications are introduced, supported by ablation studies, to handle the continuous, noisy, multivariate and multifrequency nature of time-series data. Delphyne is the first pre-trained model excelling in both general time-series and diverse financial and economic tasks such as nowcasting.

---
(References 및 Appendix A(Any-variate Attention 수식 요약은 본문에 포함)–J 세부내용은 생략. 본문 Abstract~7 Conclusions는 원문 https://arxiv.org/html/2506.06288v1 에서 전문 확보함. Appendix B(Pre-training Data 상세 목록)는 일부만 옮김.)
